# Default values for second-service.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
image:
  repository: gcr.io/my-first-project/second-service

service:
  name: second-service
  internalPort: 8080

